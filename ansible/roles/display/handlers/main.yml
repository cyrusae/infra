---
# roles/display/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart getty
  ansible.builtin.systemd:
    name: getty@{{ display_tty }}
    state: restarted
  become: true
